console.log(Math.round(1.6)); //2
console.log(Math.round(1.4)); //1

function sum(first, second) { // 매개변수 parameter
  return (first+second);
}

console.log(sum(3, 4)); // 입력값 하나하나를 의미 argument 인자
